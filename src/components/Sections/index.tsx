import { ReactElement } from "react";

type SectionsProps = {
  sectionsIds: number[];
};

const Sections = ({
  sectionsIds,
}: SectionsProps): ReactElement<SectionsProps> => {
  return <></>;
};

export default Sections;
